
エラーメッセージ

; init_gameのエラー処理
mlx_init  mlx_new_windowのチェック
load_imageの戻り値チェック
mlx_destroy_displayで開放する

順序としてはこうなります：
mlx_destroy_image() で画像をすべて破棄

mlx_destroy_window() でウィンドウを閉じる

mlx_destroy_display() で X11 接続を解放

free(mlx)（mallocされている場合）